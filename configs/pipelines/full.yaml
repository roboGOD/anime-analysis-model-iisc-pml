pipeline:
  name: full
  description: End-to-end pipeline from raw ingestion through reporting.
  stages:
    - ingest
    - profile
    - validate
    - clean
    - transform
    - build_matrix
    - select_model
    - train_model
    - assign_clusters
    - evaluate
    - diagnostics
    - plot_eda
    - plot_model
    - plot_report
